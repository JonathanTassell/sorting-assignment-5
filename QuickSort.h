#ifndef QUICKSORT_H
#define QUICKSORT_H

#include "Sort.h"

class QuickSort{
    public:
        std::vector<int> partition(std::vector<int> list);
        std::vector<int> sort(std::vector<int> list); 


}; 

#endif 